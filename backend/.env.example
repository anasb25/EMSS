NODE_ENV=development
PORT=3000

# Railway provides DATABASE_URL automatically when Postgres is linked.
# DATABASE_URL=postgresql://user:password@host:port/database

DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=emss
DB_PASSWORD=emss_password
DB_NAME=emss_db

JWT_SECRET=emss-dev-secret-change-in-production
JWT_EXPIRES_IN=1d
